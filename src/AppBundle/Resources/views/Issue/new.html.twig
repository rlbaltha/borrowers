
{% extends 'AppBundle::layout.html.twig' %}

{% block content %}

<div class="col-sm-10 ">
    <form action="{{ path('issue_create') }}" method="post"  novalidate="novalidate">
    {{ form_widget(form) }}
    <div>
        <button class="btn" type="submit">Create</button>
    </div>
</form>
</div>
{% endblock %}
