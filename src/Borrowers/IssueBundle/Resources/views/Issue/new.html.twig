
{% extends 'BorrowersHomeBundle::layout.html.twig' %}

{% block content %}

<div class="col-sm-10 ">
    <form action="{{ path('issue_create') }}" method="post" {{ form_enctype(form) }}  novalidate="novalidate">
    {{ form_widget(form) }}
    <div>
        <button class="btn" type="submit">Create</button>
    </div>
</form>
</div>
{% endblock %}
