{% extends '::prod_base.html.twig' %}

    {% block javascripts %}
        {{ parent() }}
        <script src="{{ asset('bundles/borrowershome/js/jquery.oembed.js') }}"></script>
        <script language="javascript">
            $(document).ready(function () {
                $("a.embed").oembed();
            });
        </script>
    {% endblock %}
    {% block stylesheets %}
        {{ parent() }}
        <link type="text/css" href="{{ asset('bundles/borrowershome/css/jquery.oembed.css') }}" rel="stylesheet"/>
    {% endblock %}

{% block body %}


    {{ html|raw }}

{% endblock %}